[package]
name = "cadmus"
version = "0.1.0"
edition = "2021"
description = "A byte-level BPE tokenizer trained from scratch in Rust — the merge-learning algorithm by hand, no `tokenizers` crate."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
